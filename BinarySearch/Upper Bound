int upperBound(vector<int> &arr, int x, int n){
	int s=0;
	int e=n-1;
	int ans=-1;
	int mid=s+(e-s)/2;
	if(arr[e]<=x){
		ans=e+1;
	}	
	while(s<=e){
		if(arr[mid]>x){
			ans=mid;
			e=mid-1;
		}
		else{
			s=mid+1;
		}
		mid=s+(e-s)/2;
	}
	return ans;
}
